import { BundleStatePipe } from './bundle-state.pipe';

describe('BundleStatePipe', () => {
  it('create an instance', () => {
    const pipe = new BundleStatePipe();
    expect(pipe).toBeTruthy();
  });
});
