import { BundlesFilterPipe } from './bundles-filter.pipe';

describe('BundlesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BundlesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
